import React from 'react';
import NpcEditDialog, { type NpcEditDialogProps } from '@/components/NpcEditDialog';

const NpcInstanceEditDialog = (props: NpcEditDialogProps) => <NpcEditDialog {...props} />;

export default NpcInstanceEditDialog;
